{% extends 'RofilTemplateMasterBundle:Theme:two-column.html.twig' %}

{% block content -%}
    <h2>User</h2>
    <hr>
                <div class="row">
    <div class="col-md-6">
        <a href="{{ path('manage_user') }}" class="btn btn-info">Kembali Kedaftar</a>
    </div>
    <div class="col-md-6 text-right">
                    <div class="btn-group">
                <a href="{{ path('manage_user_edit', { 'id': entity.id }) }}" class="btn btn-warning">Ubah Data</a>
                <button class="btn btn-danger" data-toggle="modal" data-target="#modalHapus">
                    Hapus Data
                </button>
            </div>
            </div>
</div>

<!-- Modal -->
<div class="modal fade" id="modalHapus" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel">Anda yakin mau menghapus data ? </h4>
      </div>
            <div class="modal-footer text-center">
        <div class="col-md-6 text-left"><button type="button" class="btn btn-default" data-dismiss="modal">No</button></div>
        <div class="col-md-6">{{ form(delete_form) }}</div>
      </div>
    </div>
  </div>
</div>

    <hr>
    
    <div class="panel panel-default">
        <table class="table table-striped table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Username</th>
                    <td>{{ entity.username }}</td>
                </tr>
                <tr>
                    <th>Password</th>
                    <td>{{ entity.password }}</td>
                </tr>
                <tr>
                    <th>Role</th>
                    <td>{{ entity.role }}</td>
                </tr>
                <tr>
                    <th>Actived</th>
                    <td>{{ entity.actived }}</td>
                </tr>
                <tr>
                    <th>Createdat</th>
                    <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
                </tr>
                <tr>
                    <th>Updatedat</th>
                    <td>{{ entity.updatedAt|date('Y-m-d H:i:s') }}</td>
                </tr>
                <tr>
                    <th>Loginat</th>
                    <td>{{ entity.loginAt|date('Y-m-d H:i:s') }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    
{% endblock %}
